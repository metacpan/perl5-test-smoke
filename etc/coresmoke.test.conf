{
    db_path           => $ENV{SMOKE_DB_PATH}     // 't/test.db',
    reports_dir       => $ENV{SMOKE_REPORTS_DIR} // 't/data/reports-tmp',
    log_level         => 'warn',
    cors_allow_origin => '*',
    app_name          => 'Perl 5 Core Smoke DB (test)',
    app_version       => '2.0',
};
