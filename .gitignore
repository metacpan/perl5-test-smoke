/local/
/data/
/log/
/.cpanm/
/cover_db/
*.swp
*.psql
*.txz
data/*.db
data/*.pid
/t/test.db
/t/test.db-wal
/t/test.db-shm
/t/data/reports-tmp/

# Vendored at build time -- real htmx is fetched by `make` (or by the
# Dockerfile.base builder stage). Don't commit a stale copy.
/public/htmx.min.js
.DS_Store
