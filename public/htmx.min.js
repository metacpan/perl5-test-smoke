/* htmx vendor bundle (placeholder).
 *
 * For local development this stub silently no-ops the data-hx-* attributes.
 * Before shipping or deploying a Docker image, replace this file with a
 * pinned htmx release, e.g.:
 *
 *   curl -fsSL https://unpkg.com/htmx.org@2.0.4/dist/htmx.min.js \
 *        > public/htmx.min.js
 *
 * The Dockerfile (plan 08) does this in the builder stage. The CI workflow
 * (plan 09 / task 17) does it before running browser-driven UI checks.
 */
console.warn("htmx placeholder loaded -- replace public/htmx.min.js with a real release before shipping.");
